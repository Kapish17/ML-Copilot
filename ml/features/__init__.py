"""Feature configuration, inference and custom transformers.

``types``         the vocabulary shared across the ML layer
``config``       ``PreprocessingConfig`` and its validation
``decisions``    per-column decisions that make feature selection explainable
``inference``    deriving a configuration from a dataset profile
``transformers`` sklearn transformers sklearn does not already provide
"""

# Development and test dependencies of the backend service.
#
# Installing this file installs the runtime dependencies too, so a development
# environment needs one command:
#
#   pip install -r backend/requirements-dev.txt
#
# The production container installs `requirements.txt` alone and therefore
# ships neither of these.
-r requirements.txt

pytest==9.1.1

# The HTTP client Starlette's TestClient is built on. Used only by the tests.
httpx2==2.12.0

# Reads the CI workflow so its jobs and steps can be asserted structurally
# rather than by matching strings in a YAML file. Test-only: no application
# code parses YAML, and the production image installs neither this nor pytest.
PyYAML==6.0.2

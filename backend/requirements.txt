# Runtime dependencies
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
pandas==3.0.5

# Reading .xlsx workbooks, through pandas' Excel reader. The only Excel
# dependency: xlrd, pyxlsb and the rest are deliberately absent, since .xls,
# .xlsb and macro-enabled workbooks are not accepted formats.
openpyxl==3.1.5

# Test dependencies
pytest==9.1.1
httpx2==2.12.0

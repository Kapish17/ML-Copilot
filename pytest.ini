[pytest]
# The repository holds two independent components with their own test suites.
# Running pytest from the repository root exercises both; `pythonpath` makes
# `app` (from backend/) and `ml` (from the root) importable without installing
# either package.
testpaths = backend/tests ml/tests
pythonpath = . backend
addopts = --strict-markers
